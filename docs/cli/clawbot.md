---
summary: "CLI reference for `joopo clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `joopo clawbot ...`
  - You need migration guidance to current commands
title: "Clawbot"
---

# `joopo clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `joopo clawbot qr` (same behavior as [`joopo qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `joopo clawbot qr` -> `joopo qr`

## Related

- [CLI reference](/cli)
